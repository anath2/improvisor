version: 2
jobs:
  build:
    working_directory: ~/Projects/improvisor
    docker:
      - image: circleci/python:3.6
    steps:
      - checkout
      - run: pip install -r requirements.txt
      - run: python -m unittest discover -v
